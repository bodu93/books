/*
 * 实现一个函数，把字符串中的每个空格替换成为"%20".
 * 例如输入"We are happy.", 则输出"We%20are%20happy."
 */
#include <string.h>

#include <iostream>

void ReplaceBlank(char str[], int length) {
	if (str == NULL || length <= 0) return;
}
